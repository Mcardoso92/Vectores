using System;

namespace EJEMPLO_VECTORES2
{
    class Program
    {
        static void Main(string[] args)
        {
            //Ejemplo con tipo BOOL (Solo se admite verdad o falso)
            // bool[] asistenciasQuincena1 = new bool[15];

            // for(int i = 0; i < 15; i++){
            //     //dia
            //     //asistencia

            // }

            bool[] asistencia = new bool[100];
            int[] legajos = new int[100];

            for (int i = 0; i < 16; i++)
            {
                for (int j = 0; j < 100; j++)
                {
                    
                }
            }
        }
    }
}
